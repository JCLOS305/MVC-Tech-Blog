const deleteResponder = require('./deleteResponder');
const updateResponder = require('./updateResponder');
const postResponder = require('./postResponder');
const responder = require('./responder');

module.exports = {
    responder,
    deleteResponder,
    updateResponder,
    postResponder
}